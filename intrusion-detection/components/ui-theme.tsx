"use client"

import { useEffect } from "react"

export function UITheme() {
  useEffect(() => {
    // Run shadcn init programmatically
    // This is just a placeholder - in a real app, you would run this command in the terminal
    console.log("Running: npx shadcn@latest init")

    // In a real implementation, you would need to run this command in the terminal
    // and not through JavaScript, as it requires user interaction
  }, [])

  return null
}

